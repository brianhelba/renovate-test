[build-system]
requires = ["uv_build==0.12.10"]
build-backend = "uv_build"

[project]
name = "renovate-test"
version = "0.1.0"
requires-python = ">=3.12"
dependencies = [
  "click==8.3.1",
  "django==4.2.28",
  "httpx",
]

[project.optional-dependencies]
test = [
  "pytest==8.4.2",
]

[dependency-groups]
dev = [
  "tox==4.61.2",
]
