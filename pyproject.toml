[build-system]
requires = ["uv_build>=0.6,<1"]
build-backend = "uv_build"

[project]
name = "renovate-test"
version = "0.1.0"
requires-python = ">=3.12"
dependencies = [
  "click>=7",
  "django>=4.2,<5",
  "httpx",
]

[project.optional-dependencies]
test = [
  "pytest==9.1.1",
]

[dependency-groups]
dev = [
  "tox==4.0.0",
]
